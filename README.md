# Chic A Boo — minimal Cloudflare Worker landing

Public paths:
- `/` — logo page
- `/logo.jpeg` — brand mark

Cloudflare Workers Builds:
- Build command: `npm install` (or leave empty after install)
- Deploy command: `npx wrangler deploy`
